# Journal: 2026-06-22

## Summary

Extracted the SQL execution framework from the AIDA repository into a standalone `sql-runner` repository, made it immediately usable as a local reference implementation, and published it to GitHub.

Repository:

- Local: `/Users/simon/Oracle/sql-runner`
- Remote: `git@github.com:simongriffiths/sql-runner.git`

## Work Completed

Created a standalone repository from the reusable AIDA SQL runner pieces:

- `bin/run-sql.sh`
- `bin/db-history-common.sh`
- `bin/db-history-init.sh`
- `bin/db-history-recent.sh`
- `bin/db-history-show.sh`
- `bin/db-history-failures.sh`
- `bin/db-history-similar.sh`

Removed AIDA-specific coupling from `run-sql.sh` by replacing the hard-coded environment-to-connection map with `config/connections.conf`.

Added local and publishable configuration patterns:

- `config/connections.conf.example` committed
- `config/connections.conf` ignored locally

Added a local config using the current AIDA-style connection names:

- `admin=admin`
- `dev=aida_dev`
- `test=app_test`
- `prod=app_prod`

Added a smoke-test SQL script:

- `examples/verify-connection.sql`

Added missing history query helpers:

- `bin/db-history-script.sh`
- `bin/db-history-commit.sh`

Added GitHub-readiness files:

- `README.md`
- `LICENSE` using MIT
- `SECURITY.md`
- `CONTRIBUTING.md`
- `Makefile`
- `docs/publishing-checklist.md`

Added offline test coverage:

- `test/run-offline-tests.sh`
- fixture log under `test/fixtures/logs/dev/runs/`

## Validation

Ran offline test suite:

```bash
make test
```

Result:

- Bash syntax checks passed.
- SQLite history schema creation passed.
- Fixture log rebuild passed.
- History query helpers passed against fixture data.
- Missing-config and unknown-environment runner validation paths passed.
- ShellCheck was skipped because it is not installed locally.

Ran live SQLcl smoke tests against the saved connection `road_blogger_dev`.

Normal mode succeeded:

- Connected user: `ROAD_BLOGGER_DEV`
- SQLcl exit: `0`
- `USER_ERRORS`: `0 rows selected`

Debug mode initially exposed a portability issue because the runner wrote through `/dev/fd/1`, which was blocked in the managed sandbox. Reworked debug output to use a safer `tee` path. Re-ran debug mode successfully against `road_blogger_dev`.

## GitHub Publication

Initial push to GitHub was rejected because the remote already had an initial commit containing `LICENSE`.

Fetched `origin/main`, confirmed the remote MIT licence matched the local licence, merged the unrelated remote initial commit, and pushed successfully.

Current commit history:

- `e67bcbb Merge remote-tracking branch 'origin/main'`
- `af10e73 Initial commit`
- `77f966d Add MIT license`
- `b1be07f Prepare SQL runner for publication`
- `b105e60 Extract SQL runner reference implementation`

Local `main` now tracks `origin/main`.

## Current State

The repository is usable as a reference implementation and is published to GitHub.

Expected ignored local runtime files:

- `.codex/`
- `config/connections.conf`
- `logs/`

## Follow-Up Ideas

- Install ShellCheck locally or add it in CI so `make test` performs full linting.
- Add GitHub Actions for `make test`.
- Consider adding `bin/sql-runner-doctor.sh` to check SQLcl, sqlite3, config validity, and connection reachability.
- Consider adding a one-off `--connection` option for ad hoc smoke tests without a config file.
- Consider adding `--no-history` for read-only or constrained CI environments.
