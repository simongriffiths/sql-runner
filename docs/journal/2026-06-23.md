# Journal: 2026-06-23

## Summary

Packaged `sql-runner` as a companion Codex skill while keeping the GitHub repository as the source of truth for the executable framework.

## Work Completed

Added a repo-local skill under:

```text
skills/sql-runner/
```

The skill includes:

- `SKILL.md` with the required sql-runner operating workflow
- `agents/openai.yaml` for Codex UI metadata
- `references/workflow.md` for installation, production safety, and post-run review details
- `scripts/install-sql-runner.sh` to install the runner into another project from either a local checkout or GitHub

Installed the same skill globally at:

```text
/Users/simon/.codex/skills/sql-runner
```

Updated `Makefile` so `make test` syntax-checks the skill installer script.

## Validation

Validated:

- `SKILL.md` frontmatter parses as YAML
- `agents/openai.yaml` parses as YAML
- skill installer script passes `bash -n`
- installer can install from the local checkout into a temporary target directory
- `make test` passes

The official skill quick validator could not run because the local Python runtimes do not have PyYAML installed. Practical validation covered the skill files and installer behavior instead.

## Design Decision

The skill is intentionally thin. It teaches Codex how to install and operate `sql-runner`, but it does not duplicate the entire framework. The GitHub repo remains the source of truth for the runner scripts, docs, tests, and releases.
